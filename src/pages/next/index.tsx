import React from 'react'

const next = () => {
  return (
    <div>if you want to write code inside other folder than create a folder in pages and index.tsx file and on url put folder name </div>
  )
}
export default next